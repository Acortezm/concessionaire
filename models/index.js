const product_model = require('./product.model');
const ventas_model = require('./ventas.model');
const usuarios_model = require('./usuarios.model');

module.exports = Object.freeze({
    product_model,
    ventas_model,
    usuarios_model
})
