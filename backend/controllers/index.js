const product_controller = require('./product.controller');
const ventas_controller = require('./ventas.controller');
const usuarios_controller = require('./usuarios.controller');

module.exports = Object.freeze({
    product_controller,
    ventas_controller,
    usuarios_controller
})